<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
    "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
  <head>
    <title>John Gardner Center</title>
	<link rel="stylesheet" type="text/css" href="../stylesheets/style.css" />
    <link rel="stylesheet" type="text/css" href="../stylesheets/user.css" />
    <link rel="stylesheet" type="text/css" media="print" href="../stylesheets/print.css" />
    <link rel="stylesheet" type="text/css" href="../stylesheets/p7tmcoollines.css" />

  </head>

  <a target="_blank" id="feedback-tab" href="userComment"></a>
<img src="../images/JGC.gif" alt="John Gardner Center Logo" />
  
  
<div id ="content">
		<div align="right">
			<table border="1">
				<tr>
					<td class="right">
						<%  hDisplay = false
							if(session[:user] && session[:user].admin && session[:user].affiliateOrg)
								concat "<a href = ../../user/welcomeAdminUser >Home </a>"
							end
							if(session[:user] && session[:user].admin && !session[:user].affiliateOrg)
								concat "<a href = ../../user/welcomeUser >Home </a>"
							end
							if(session[:user] && !session[:user].admin && session[:user].affiliateOrg)
								
								concat "<a href = ../../user/welcomeOrgUser >Home </a>"
							end
							if(session[:user] && !session[:user].admin && !session[:user].affiliateOrg)
								concat "<a href = ../../user/welcomeUser >Home </a>"
							end
							
						%>
						
						
					</td>
					<td class="right">
					<%
				  		if (session[:user])
							cDisplay = "logout"
				  		else
				    		cDisplay = "login"
				  		end
					%>
					<a href="/user/<%= cDisplay%>" ><%= cDisplay%> </a>
				</td>
			</tr>
			
			</table>
		</div>
	</div>
	<div id ="content">
		<table border="1">
			<tr>
				<td class="centered">					
					<div>
					<a href="../index.html"> About Us </a>
					</div>
				</td>
				
				<td class="centered">
					<div>
					<a href="../organizations">Organizations </a>
					</div>
				</td>
				<td class="centered">					
					<div>
					<a href="../programs.html">After School Programs </a>
					</div>
				</td>
				<td class="centered">					
					<div>
					<a href="../parents.html">Parents</a>
					</div></td>
				<td class="centered">					
					<div>
					<a class="p7currentmark" href="../programdirectors.html">Program Director </a>
					</div></td>
				<td class="centered">					
					<div>
					<a href="../user/search">Search </a>
					</div></td>
			</tr>
		</table>
			</div>
		<br>
		<br>

  <br />
  <br/>
  <div id="loginbox">
  	<div class="copy">
  		<h3> Browse results by category </h3>
		<hr style="color:#CCC"/>
	</div>

		<table>
			<tr>
				<td class="left">
					<h3> Cost </h3>
					<% @statistic.costs.sort.each do  |key,value| %>
					<%= link_to " #{key}(#{value})", :action => "CostFilterOnCategory", :cost => "#{key}", :category => "#{@category}" %>
					<br/>
					<% end %>
					<h3> Duration </h3>
					
					<% @statistic.duration.sort.each do  |key,value| %>
					<%= link_to " #{key}(#{value})", :action => "DurationFilterOnCategory", :duration => "#{key}", :category => "#{@category}" %>

				
					<br/>
					<% end %>
					<h3> Age Group </h3>
					<% @statistic.age_group.sort.each do  |key,value| %>
					<%= link_to " #{key}(#{value})", :action => "AgeFilterOnCategory", :age => "#{key}", :category => "#{@category}" %>
					<br/>
					<% end %>
					
				</td>
			<div id ="content">
		
				<div class ="copy">
				<td class="left">
					Total number of results: <%= @activities.size() %></p>
					<br/>
				    <br/>
					<%for activity in @activities %>
					<%= link_to activity.name, activity %><br/>
					<%=activity.description%> <br/><br/>
					<% end%>
				</td>
				</div>
			</div>	
			</tr>
			
			
	</table>


<%= link_to 'Back', :back %>

</div>
