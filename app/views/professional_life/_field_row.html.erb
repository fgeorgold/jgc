<!-- Partial for the field row. -->

<tr>
  <td class="label"><%= field_row.humanize %> :</td>
  
  <!-- Trick: Use letter h. This is the minimalist Rails function for HTML
              escape, which replaces HTML tags with their escaped versions
			  (e.g., &lt; for <). -->
  <!-- Trick: @pd_user.professional_life[field_row] is the same as @pd_user.professional_life.<field_row> -->
  <td><%= h @pd_user.professional_life[field_row] %></td>
</tr>