<a target="_blank" id="feedback-tab" href="../user/userComment"></a>
<img src="/images/JGC.gif" alt="John Gardner Center Logo" />

<div id ="content">
		<div align="right">
			<table border="1">
				<tr>
					<td class="right">
						<%  hDisplay = false
							if(session[:user] && session[:user].admin && session[:user].affiliateOrg)
								concat "<a href = ../../user/welcomeAdminUser >Home </a>"
							end
							if(session[:user] && session[:user].admin && !session[:user].affiliateOrg)
								concat "<a href = ../../user/welcomeUser >Home </a>"
							end
							if(session[:user] && !session[:user].admin && session[:user].affiliateOrg)
								
								concat "<a href = ../../user/welcomeOrgUser >Home </a>"
							end
							if(session[:user] && !session[:user].admin && !session[:user].affiliateOrg)
								concat "<a href = ../../user/welcomeUser >Home </a>"
							end
							
						%>
						
						
					</td>
					<td class="right">
					<%
				  		if (session[:user])
							cDisplay = "logout"
				  		else
				    		cDisplay = "login"
				  		end
					%>
					<a href="/user/<%= cDisplay%>" ><%= cDisplay%> </a>
				</td>
			</tr>
			
			</table>
		</div>
	</div>
	
	<div id ="content">
		<table border="1">
			<tr>
				<td class="centered">					
					<div>
					<a href="../index.html"> About Us </a>
					</div>
				</td>
				
				<td class="centered">
					<div>
					<a class="p7currentmark" href="organizations">Organizations </a>
					</div>
				</td>
				<td class="centered">					
					<div>
					<a href="programs.html">After School Programs </a>
					</div>
				</td>
				<td class="centered">					
					<div>
					<a href="parents.html">Parents</a>
					</div></td>
				<td class="centered">					
					<div>
					<a href="programdirectors.html">Program Director </a>
					</div></td>
				<td class="centered">					
					<div>
					<a href="user/search">Search </a>
					</div></td>
			</tr>
		</table>
			</div>
		<br>
		<br>
		
				
				
		<div id="loginbox">
			<div class="copy">
				<p> Please enter information for the new organization below </p>
				<hr style="color:#CCC"/>
		  	</div>
			
			<div id ="content">
				<%= form_tag :action=> "create" %>
				<%= error_messages_for 'organization' %><br/>
				<label for="organization_name">Name</label><br/>
				<%= text_field "organization", "name", :size => 20 %><br/>
				
				<label for="organization_website">Website</label><br/>
				<%= text_field "organization", "website" %><br/>

				<label for="organization_description">Description</label><br/>
				<%= text_area "organization", "description" %><br/>

				<label for="organization_resources">Resources</label><br/>
				<%= text_area "organization", "resources" %><br/>

				<label for="organization_contact">Contact</label><br/>
				<%= text_field "organization", "contact" %><br/>

				<label for="organization_email">Email</label><br/>
				<%= text_field "organization", "email" %><br/>
				<div id="partners">
				<p><label for="organization_partner_attribute__partner_org_id">Partners: </label><br/> 
				
				<%= render :partial => "add_partner" %>
				</p>
				</div>
				<%= link_to_function "Add a partner" do |page|
					page.insert_html :bottom, :partners, :partial => 'add_partner', :object => Partner.new
				end %>
				<br/> 
				<%= submit_tag "Create" %>
				
			</div>
			<%= link_to 'Back', :back %>
		</div>